# Spruce.Client

First-party .NET client for Spruce. It supports opaque binary publishing, binary
batches, idempotent retry, concurrent streaming consumption, batched ACK/NACK,
deduplication, diagnostics, Bearer and Basic authentication, and standard `HttpClient`
TLS configuration.
